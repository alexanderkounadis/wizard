import { WizardHeaderRefDirective } from './wizard-header-ref.directive';

describe('WizardHeaderRefDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new WizardHeaderRefDirective();
  //   expect(directive).toBeTruthy();
  // });
});
